import styled from "styled-components";
import { mainColor } from "../../styles/variables";

export const SidebarRightWrapperStyled = styled.header`
  position: relative;
  background-color: ${ mainColor };
`;
